# pantheon-guide — Claude Code Context

## What this is
A League of Legends pre-game itemization and rune guide. Single-page React app (no backend, no router, no state library). User selects champion → role → enemy class → ahead/behind → gets item recommendations + interactive rune page.

## Stack
- React 19 + Vite 8, JSX only, no TypeScript
- 100% inline styles + src/theme.js for style tokens
- No CSS framework, no Redux, no external UI libraries
- Run: `npm run dev` from project root

## Key files
- `src/App.jsx` — all UI (~1670 lines): helpers, CLASSES, RUNE_TREES, RunePage, panels
- `src/data/champs/*.js` — one file per champion (rune constants + default-exported champ object); `index.js` assembles the CHAMPS array. Shared rune constants must be exported/imported (e.g. RENEKTON_TOP_DEFAULT_RUNES) — a `vite build` does NOT catch a missing cross-file constant, it only fails at runtime
- `src/data/runeHelpers.js` — `I(name, why)` item-entry helper used by all champ files
- `src/theme.js` — all colour tokens and reusable style objects
- `electron/main.cjs` — desktop shell; serves dist/ over app:// (MIME map required for module scripts). `npm run dist:win` builds installer + portable exe into release/
- `public/ddragon/` — Data Dragon assets (populated by helpers/setup_ddragon.py)
- `helpers/setup_ddragon.py` — run after each patch: `python helpers/setup_ddragon.py` (no args = pulls latest patch from the noxelisdev/LoL_DDragon GitHub mirror, incremental — skips images already on disk; `--force` re-downloads). Passing a dragontail `.tgz` path still works as offline fallback. NEVER `git clone` that mirror — it is ~10 GB. After running it, regenerate the LCU id tables: `node helpers/gen-lcu-data.mjs` (it pins to `public/ddragon/version.txt`, NOT always-latest, so the export ids always match the bundled assets).
- **Auto patch updater** — `helpers/check-patch.mjs` compares the bundled version to the official Data Dragon API; `.github/workflows/patch-update.yml` runs it on a cron (every 3 days) and, on a new patch, runs setup_ddragon + gen-lcu-data + build and opens a PR (branch `auto/ddragon-<ver>`). The PR still needs a human pass for new champions / item reworks / meta shifts before merge. One-time repo setting required: Actions → General → allow GitHub Actions to create PRs.

## Architecture rules (must follow)
1. All sub-renderers inside RunePage MUST be called as plain functions `{primaryContent()}` NOT JSX components `<PrimaryColumn />` — the latter causes React to remount on every render, breaking images and state
2. After any edit to champion data objects, validate brace balance: `python3 -c "d=sum(1 if c=='{' else -1 if c=='}' else 0 for c in open('src/App.jsx').read()); print('Balance:', d)"`
3. `runeImg` must guard against null src: `{src && !imgFail(ek) ? <img src={src} onError={...}/> : <Fallback />}` — never `<img src={null}>`
4. All rune/perk images in `/ddragon/img/perk-images/` are `.png`, not `.svg`

## Image paths
- Champions: `/ddragon/img/champion/{DDkey}.png` (via `toDD()` sanitiser)
- Items: `/ddragon/img/item/{numericId}.png` (looked up from `item.json` at runtime)
- Runes: `/ddragon/img/{path}` where path comes from `runesReforged.json` flat map
- Stat shards: `/ddragon/img/perk-images/StatMods/StatMods{Name}Icon.png` (hardcoded SHARD_ICONS)
- Role icons: `/images/roles/position-{role}.svg`

## Data structure — adding a champion
```js
// 1. Add rune constant before const CHAMPS = [
const CHAMPNAME_ROLE_DEFAULT_RUNES = { keystone, primary, primaryRunes, secondary, secondaryRunes, shards, reason };

// 2. Add to CHAMPS array — multi-role uses roles:{}, single-role uses flat data:{}
{ id:"champname", display:"ChampName", dd:"DDKey", color:"#hex", glow:"#hex",
  roles: { Top: { role, corePath, coreNote, sideItems, data: { JUGGERNAUT:{ahead:[I(...)x3],behind:[I(...)x3],runes:{...DEFAULT}}, ... all 13 classes } } } }
```

## Meta-build verification (in progress, 2026-07-18)
Auditing every champion/role's `corePath` + rune page against live patch-26.14 build data (item choices in early sessions were sometimes based on general knowledge/pattern-reuse rather than verified searches, and drift for real — e.g. Briar Jungle had no Eclipse at all in current meta, Lee Sin's real build is Eclipse-based not Trinity Force, Teemo now rushes Statikk Shiv over Nashor's Tooth). Confirmed/fixed so far: Briar (Jungle), Lee Sin (Jungle), Yasuo (added Top + Bot roles), Udyr (added Top role), Pantheon (all 4 roles), Teemo (all 4 roles). Remaining ~31 champions not yet re-verified this pass — treat their builds as inherited-knowledge quality, not confirmed-current, until swept.

### Build/rune sweep — verified against live sites (u.gg/mobalytics/lolstats, patch 26.15)
**Batch 1 (2026-08-09):** searched each champ's current build+runes and compared. **Fixed:** Zed Mid (was First Strike/Inspiration → Electrocute/Domination + Sorcery; core Eclipse-first), Yasuo Mid (Domination secondary → Resolve, Triumph → Absorb Life), Yone Mid+Top (Triumph → Absorb Life; BOTRK-first core), Wukong Top (Inspiration secondary → Resolve Bone Plating/Unflinching; core → Trinity/Steelcaps/Black Cleaver/Death's Dance), Pantheon Jungle (→ Conqueror + Inspiration Magical Footwear/Cosmic Insight — the Hextech Flashtraption/Domination pages were BOTH wrong; core Eclipse → Guardian Angel), Mel Mid (core Rocketbelt → Zhonya's). **Verified already-correct (no change):** Volibear (T/J), Viego (J), Akali (M/T — Hextech Gunblade IS meta first item), Fiddlesticks (J), Naafiri (J/M), Zoe (M), Locke (M), Wukong Jungle, Mel runes. Method: WebSearch the champ+role build, compare corePath + keystone/primary/secondary, fix the rune constant (shared across all 13 class entries) + corePath, verify `buildRunePayload` stays valid.

**Batch 2 (2026-08-09):** **Fixed:** Thresh (Resolve → Bone Plating/Unflinching, secondary Domination → Inspiration Biscuit Delivery/Cosmic Insight), Leona (same Resolve fix + Approach Velocity → Biscuit Delivery; core add Knight's Vow), Ahri (Cheap Shot → Taste of Blood), Darius (Grasp → Conqueror + Sorcery secondary; core → Youmuu's Ghostblade-first tempo build ~54% WR). **Verified already-correct:** Jinx, Caitlyn, Kai'Sa, Ezreal (Bot data all solid), Lux (M/S), Morgana (S/M), Katarina (M), Master Yi (J), Garen (T — runes match; Swiftmarch boots a valid variant). ~150 champ-roles still to sweep.

## Current champion roster (173 of 173 — COMPLETE)
Pantheon (Top/Mid/Support/Jungle), Teemo (Top/Mid/Support/Jungle), Renekton (Top/Jungle off-meta), Aurelion Sol (Mid), Katarina (Mid), Xerath (Mid/Support/Top off-meta), Sion (Top/Mid/Support/Jungle), Wukong (Top/Jungle), Rek'Sai (Jungle), Shyvana (Jungle/Top), Warwick (Jungle/Top off-meta), Udyr (Jungle AD bruiser/Top off-meta), Sett (Top/Jungle), Volibear (Top tank / Jungle bruiser), Nocturne (Jungle), Briar (Jungle/Top), Locke (Mid AP assassin), Jinx (Bot), Thresh (Support), Darius (Top), Lee Sin (Jungle), Ahri (Mid), Lulu (Support), Leona (Support), Yasuo (Mid/Top/Bot), Swain (Mid/Support), Pyke (Support/Mid off-meta), Seraphine (Support/Bot), Gragas (Jungle/Top/Mid off-meta), Karma (Support/Mid), Ziggs (Mid/Bot off-meta), Yone (Mid/Top), Zac (Jungle/Support/Top off-meta), Zed (Mid), Yorick (Top/Jungle off-meta), Garen (Top), Zaahen (Top/Jungle off-meta), Yunara (Bot), Mel (Mid), Ambessa (Top/Jungle off-meta), Olaf (Jungle/Top), Jax (Top/Jungle off-meta), Irelia (Top/Mid), Kindred (Jungle only — never verify/add other roles for her, she has near-0% real pick rate anywhere else), Caitlyn (Bot), Ezreal (Bot), Vayne (Bot/Top off-meta), Kai'Sa (Bot), Draven (Bot), Blitzcrank (Support), Nami (Support), Riven (Top/Mid), Vi (Jungle), Akali (Mid/Top off-meta), Fiddlesticks (Jungle/Mid/Support/Top — a rare 4-role champ, all confirmed real with distinct builds), Veigar (Mid/Support), Zoe (Mid), Viktor (Mid), Lissandra (Mid), Naafiri (Jungle/Mid), Fizz (Mid/Jungle off-meta), Jhin (Bot), Ashe (Bot/Support), Samira (Bot), Nautilus (Support), Morgana (Support/Mid off-meta), Renata Glasc (Support), Miss Fortune (Bot), Xayah (Bot), Lucian (Bot), Twitch (Bot), Sivir (Bot), Tristana (Bot), Braum (Support), Soraka (Support), Rakan (Support), Alistar (Support), Janna (Support), Kayn (Jungle), Master Yi (Jungle), Elise (Jungle/Support — Support added in the alt-role audit: B tier 51.0%, Electrocute/Bloodsong kill-lane page), Viego (Jungle), Camille (Top/Support — Support added in the alt-role audit: B tier at 7.7% pick / 44k matches, a real meta role, Hail of Blades/Bloodsong/Sundered Sky dive page), Fiora (Top), Aatrox (Top/Jungle — Jungle added in the alt-role audit: A tier 52.9% rank 16, Hubris-first with Inspiration secondary), Orianna (Mid), Syndra (Mid), Senna (Support/Bot dual-primary), Varus (Bot/Top — Top added in the alt-role audit: off-meta 49.6% but genuinely distinct Press the Attack/Experimental Hexplate bruiser build), Karthus (Jungle/Mid), Poppy (Top/Support), Maokai (Jungle/Support), Malphite (Top/Jungle/Support — a 3-role flex icon), Shen (Top/Support), Amumu (Jungle/Support), Azir (Mid), Ryze (Mid), Vladimir (Mid), Kassadin (Mid), LeBlanc (Mid — file/id `leblanc.js`, dd:"Leblanc" lowercase b), Sylas (Mid/Jungle — Jungle confirmed at user prompt: S+ tier, rank 1 of 77 junglers on lolalytics, Conqueror + Inspiration-secondary page distinct from Mid's Resolve), Ekko (Mid/Jungle), Talon (Mid/Jungle — Jungle is a genuinely distinct Fleet Footwork/Umbral Glaive build, S tier), Diana (Jungle/Mid — distinct pages: Conqueror bruiser jungle vs Electrocute assassin mid), Lux (Mid/Support — same runes both roles, different item economics), Zyra (Support/Jungle — Jungle confirmed at user prompt: 51.4% WR, Electrocute + Liandry's farm build, distinct from Comet support page), Sona (Support), Rengar (Jungle), Shaco (Jungle — meta build is AD crit, not AP), Nidalee (Jungle), Gnar (Top), Kalista (Bot), Zeri (Bot), Brand (Support/Mid — Jungle skipped, 45.9% WR 0.3% pick), Taliyah (Mid/Jungle — fully distinct pages: Deathfire Touch control mage vs Dark Harvest burst ganker), Jayce (Top — Mid skipped, D tier + identical runes), Tahm Kench (Support/Top — file/id `tahmkench.js`, dd:"TahmKench"; same Grasp runes both roles, items differ: utility tank Support vs Heartsteel/Riftmaker offense Top), Sejuani (Jungle/Top — fully distinct pages: Aftershock teamfight vs Grasp/Demolish lane), Aurora (Mid/Top — same Electrocute page both, like Lux/Senna), Smolder (Bot), Vel'Koz (Support/Mid — file/id `velkoz.js`, dd:"Velkoz"; same Comet page both roles, Support is the higher-WR primary), Graves (Jungle — Top skipped, D tier 0.2% pick), Kha'Zix (Jungle — file/id `khazix.js`, dd:"Khazix", First Strike economy page), Jarvan IV (Jungle — file/id `jarvaniv.js`, dd:"JarvanIV"), Mordekaiser (Top/Jungle — Jungle is OM-label but 53.3% WR B-tier on lolalytics, identical page to Top, Morgana-precedent add), K'Sante (Top — file/id `ksante.js`, dd:"KSante"), Hwei (Mid/Support — distinct pages: Deathfire Touch mid vs Arcane Comet support), Rell (Support — S tier rank 4, Aftershock + Hextech Flashtraption engage page), Kog'Maw (Bot — file/id `kogmaw.js`, dd:"KogMaw", on-hit Lethal Tempo + Resolve durability splash), Cassiopeia (Mid/Bot — she can NEVER buy boots, her passive forbids it; same RoA/Archangel's build both lanes, Karthus precedent), Twisted Fate (Mid — file/id `twistedfate.js`, dd:"TwistedFate"; ADC skipped OM 47.5%), Neeko (Support/Mid — distinct builds: Zaz'Zak's utility vs Rocketbelt/Stormsurge flank), Vex (Mid — A tier anti-dash Electrocute), Gwen (Top — Jungle skipped, C 49.7% near-identical build), Nasus (Top/Jungle — Jungle is A-tier 52-54% with fully distinct Protoplasm Harness/Iceborn build + Approach Velocity rune), Ornn (Top), Hecarim (Jungle — Stormraider's Surge speed page, Shojin/Cleaver/Death's Dance/Endless Hunger), Evelynn (Jungle), Bard (Support — meta page is Electrocute/Deep Ward Domination, not a utility page; Bandlepipes is his new-season item), Kayle (Top — Mid has no current data), Kennen (Top — Mid skipped, D-tier identical build), Rumble (Top — Jungle skipped 46.1%; Bloodletter's Curse core), Lillia (Jungle — Top dead, no data), Nunu & Willump (Jungle — file/id `nunu.js`, dd:"Nunu"; meta build is AP burst Rocketbelt/Stormsurge Dark Harvest, NOT tank), Xin Zhao (Jungle — file/id `xinzhao.js`, dd:"XinZhao"), Aphelios (Bot — PTA crit w/ Cash Back/Triple Tonic), Milio (Support — Dream Maker/Helia enchanter), Annie (Mid/Support — same Electrocute page both, Manaflow swap in Support, Zaz'Zak's economy), Malzahar (Mid — S+ tier, passive spell-shield safety pick), Galio (Mid/Support — fully distinct pages: Stormraider's Rocketbelt playmaker vs Aftershock Celestial Opposition engage), Heimerdinger (Top — his lolalytics primary lane now; Conqueror turret page; Support skipped, contradictory 52.3%-vs-46.2%/212-match data), Bel'Veth (Jungle — file/id `belveth.js`, dd:"Belveth"; S tier rank 4, Kraken/Stridebreaker), Skarner (Jungle — Grasp/Heartsteel + Approach Velocity; Top no data), Nilah (Bot — 53-55% WR melee-crit Conqueror), Taric (Support — Glacial Augment/Inspiration + Winter's Approach), Anivia (Mid — current page is Electrocute/RoA, not old scaling pages; Support skipped 46.4%), Qiyana (Mid — Jungle skipped 46.5-47.8%), Akshan (Mid — Kraken/Hexoptics C44 marksman-mid), Gangplank (Top — S tier rank 6, 52.4% on 31.8k matches, Deathfire Touch/Essence Reaver crit), Urgot (Top — PTA/Cleaver + Overlord's Bloodmail), Dr. Mundo (Top/Jungle — file/id `drmundo.js`, dd:"DrMundo"; distinct pages: Grasp/Warmog's top vs Fleet/Heartsteel/Titanic jungle), Rammus (Jungle — S/A 53.1%, Aftershock/Thornmail counter-tank), Yuumi (Support — C tier but roster-complete pick; like Cassiopeia she NEVER buys boots, attached movement replaces them), Corki (Mid — Conqueror/Trinity/Manamune mixed damage), Kled (Top — B+ 53.1%, Titanic/Overlord's Bloodmail), Illaoi (Top — S 51.7%, Grasp/Cleaver/Iceborn), Singed (Top — S tier rank 6, 53.6%, Deathfire Touch proxy; bootsline is Swiftness-first), Tryndamere (Top — current page is Hail of Blades/Domination, not old LT), Cho'Gath (Top — file/id `chogath.js`, dd:"Chogath"; modern build is AP Hail of Blades/Rocketbelt/Riftmaker), Trundle (Jungle — PTA/Trinity; Top skipped 48.1%), Quinn (Top — A 53-54%, Electrocute/Profane Hydra), Ivern (Jungle — B 52.2%, full enchanter build Redemption/Moonstone/Ardent), Zilean (Support — A- 53.0%, Aery/Shurelya's/Imperial Mandate)

**Alt-role audit (2026-07-18, user-prompted):** the user spotted that batch-8 "limit test" champs never got alt-role checks. Audit ran 15 candidate roles; 4 passed and were added (Elise Support, Camille Support, Varus Top, Aatrox Jungle — see entries above). 11 researched-and-skipped with data: Master Yi Top (D 48.9%, off-meta sample 45.3%), Kayn Mid (D 47.9%, 0.0% pick), Shaco Top (45.9-47.5%, 0.1% pick), Shaco Mid (no data), Smolder Top (D 44.3% rank 103), Twitch Support (D 43.2%), Twitch Jungle (no current data), Tristana Mid (no data), Lucian Mid (no data), Viego Top (48.6% stale-patch data only), Miss Fortune Support (D/OM 39.6-44.6%). Previously settled skips reconfirmed: LeBlanc Support (49.6%, 413-match sample), Jayce Mid (D, identical runes), Shaco Support (D 47.9%). Don't re-research these without a patch-shift reason.

All 173 are complete (roster FINISHED 2026-07-18 — the roadmap target is met; future roster work is patch-sweep maintenance and newly released champions only): every role has all 13 enemy classes filled in (`data:{}` covers JUGGERNAUT through SPECIALIST) with ahead/behind items and a `runes:` block. Verify this after any data edit — a missing class entry doesn't fail the build, it just renders blank for that role+class combo (this caused a real bug: role switching used to show stale/blank rune pages because several champs had only 2-9 of 13 classes filled in). Roadmap target is 173 champions total; new champs should ship with full multi-role depth from the start, including off-meta/secondary roles where the champion is genuinely played there (e.g. Pyke Mid, Ziggs Bot, Zac Support) — each role gets its own `bans`/`replacements`/`corePath`/rune constant, not a shared fallback. `"Bot"` is a valid role key (icon: `position-bottom.svg`) alongside Top/Jungle/Mid/Support. Zaahen (Top/Jungle bruiser, released patch 25.23) is the newest Darkin; Yunara (Bot ADC), Mel (Mid mage, released alongside Ambessa's Arcane tie-in), Ambessa (Top/Jungle bruiser), and **Locke, the Ashen Exorcist** (Mid AP assassin, released patch 26.13, Q/W nerfed in 26.14 — nail damage, stack bonus damage, recast hold time, and Grey Health cap all reduced) are recent releases not yet in most contributors' general knowledge — their kits/roles were confirmed via WebSearch, not inherited pattern-reuse or assumption. Every lane now has 20+ champion-role entries (a large single-session batch of 20 champions — Bot: Miss Fortune/Xayah/Lucian/Twitch/Sivir/Tristana, Support: Braum/Soraka/Rakan/Alistar/Janna, Jungle: Kayn/Master Yi/Elise/Viego, Top: Camille/Fiora/Aatrox, Mid: Orianna/Syndra — closed the Bot/Support gap that prior batches were tracking); future "pick the next batch yourself" calls should re-run the per-role audit rather than assume the old imbalance still holds. `renataglasc.js` is the file/id key for Renata Glasc (matches her existing `"Renata"` DD_OVERRIDES entry in App.jsx).

## Draft data (per role)
`bans:[3 champ names]` (worst matchups — recommended bans) and `replacements:[3 champ names]` (pocket picks if banned; roster champs preferred — they render as clickable chips in the "Playing as" bar that switch the active champion). Multi-role champs carry them inside each `roles:{Top:{bans,replacements,...}}` block (matchups differ per lane); single-role champs keep them at champ level as fallback — the tiles read `activeChampRole.bans || champ.bans`. Names must be Data Dragon display names (resolved via `toDD()`/DD_OVERRIDES).

## 13 enemy classes
JUGGERNAUT, DIVER, ASSASSIN, SKIRMISHER, BURST_MAGE, BATTLEMAGE, ARTILLERY, MARKSMAN, ENCHANTER, CATCHER, VANGUARD, WARDEN, SPECIALIST

## Rune trees (patch 26.14)
- Precision row 1: Absorb Life, Triumph, Presence of Mind (NOT Overheal)
- Precision row 2: Legend: Alacrity, Legend: Haste, Legend: Bloodline (NOT Tenacity)
- Domination row 2: Sixth Sense, Grisly Mementos, Deep Ward (renamed from Zombie/Ghost/Eyeball)
- Stat shards row 3: Health (scaling), Tenacity, Health (NO Armor or Magic Resist)

## Desktop app
**Tray companion behaviour** (`electron/main.cjs`): the app runs from the Windows system tray (hidden-icons). Closing the window minimises to tray (`close`→`preventDefault`+`hide`), it does NOT quit — quit is only via the tray menu (which sets `isQuiting`). `window-all-closed` intentionally does nothing. Auto-launch at login is the OS login item (`app.setLoginItemSettings({openAtLogin, args:['--hidden']})`, only written when `app.isPackaged`); the `--hidden` arg makes the login launch start in the tray (`START_HIDDEN`). The poll loop watches `getCreds()` for a false→true transition (League client just launched) and calls `revealForLeague()` → `positionWindow()`: a **second monitor** if `screen.getAllDisplays().length>1` (centres on the non-primary display), else **docked to the right ~42%** of the single screen beside League. Tray icon is `electron/tray-icon.png` (regenerate via `node helpers/gen-tray-icon.mjs`). Renderer controls it via `window.frge.getStartup()/setStartup()` (Settings → Startup toggle).

Electron shell in `electron/main.cjs`. `npm run app` runs against built dist/; `npm run app:dev` loads the Vite dev server (run `npm run dev` first); `npm run dist:win` produces `release/Pantheon Guide Setup <ver>.exe` (installer) + `release/Pantheon Guide <ver>.exe` (portable). electron-builder uses `electronDist: node_modules/electron/dist` (its own download/rename step fails on this machine). NOTE: this dev environment sets ELECTRON_RUN_AS_NODE=1 — clear it before launching any Electron binary or `require("electron")` returns a path string and the app instantly exits.

## Roadmap
- Export rune page as Riot runeset JSON for in-game import
- **Custom Build Studio + local profiles** (user vision, 2026-08-09 — pipeline, mostly client-only buildable now): let users author their OWN build (pick champion + role, edit the rune page — already editable — and assemble an item set: start / core / situational), name it, and save it. **Export as a shareable code**: serialise {champ, role, runes, items} to a compact versioned string (e.g. `FRGE1-<base64url(JSON)>`, or a packed binary for shorter codes) that pastes into Discord/chat; **import** parses it back. **Import to client** reuses the existing LCU pipeline (`buildRunePayload` + item set → `applyBuild` in `main.cjs`) — no new client plumbing. **Local storage FIRST** (no backend/auth needed): the Electron main process reads/writes build JSONs in a **`FRGE.GG` folder** — prefer a user-visible `Documents/FRGE.GG/builds/*.json` (fall back to `app.getPath('userData')`), via new IPC `frge:save-build` / `list-builds` / `load-build` / `delete-build`. **Local profiles** = multiple named local profiles (a folder each) holding saved builds + the prefs currently in `localStorage` (theme, favourite champ, startup, champ-select toggles) — the interim "profile" until real accounts exist. **Migration path:** when the backend + Riot RSO land (see Monetisation/accounts — the "secure way to create user profiles" the user is waiting on), sync these local builds/profiles to the cloud; store any tokens in Electron `safeStorage`, never `localStorage`. Build order: (1) local folder read/write + a Saved-Builds list, (2) the custom build editor, (3) the export/import code, (4) cloud sync once accounts exist.
- **Settings / preferences expansion** (user vision, 2026-08-06 — partially started): grow the top-right Settings popout into a fuller preferences surface. Sections envisioned: **Startup** (done — launch-on-startup/tray), **Layout** (let users customise the workspace — panel arrangement, which columns show, ad/side-rail prefs, compact vs roomy), **Integrations → Riot account** (backend + RSO gated — see Monetisation/accounts; the Account section is the entry point), **Integrations → Discord** (Rich Presence "playing FRGE / in champ select" is doable client-side via a Discord RPC lib in the Electron main process; deeper Discord bot/login is backend-gated), **Champ-select** (surface the existing auto-hover / pre-hover / auto-sync toggles here too, currently only in the live bar), **Theme** (done). When it outgrows the 290px popout, promote it to a proper tabbed settings modal. Build the client-only pieces first (Layout, Discord RPC, mirroring the champ-select toggles); Riot account waits on the backend.
- Live game data: poll League Client APIs from the Electron main process — champ select via LCU (`lockfile` port/token, `/lol-champ-select/v1/session`) and in-game via Live Client Data API (`https://127.0.0.1:2999/liveclientdata/allgamedata`, self-signed cert) — then recommend picks/builds against the revealed enemy team
- Rule engine: TRAITS + CLASS_RULES for auto-generating item fallbacks
- Riot API integration for Challenger data
- LLM-driven builds for all 5 enemy champions simultaneously
- **Item "good against" spider/radar graph** (desktop — idea stage): on each build item, a small radar chart pointing at which enemy champions / classes it's strongest into (e.g. Serpent's Fang → shield comps, Thornmail → AD healers). Needs a per-item effectiveness dataset keyed by enemy class (the 13 classes) or champion; can seed from the existing counter logic in `enemyTeam.js` / `itemCounters.js` rather than authoring from scratch.
- **Desktop auto-update — SHIPPED v0.7.0 (2026-08-07).** `electron-updater` + `electron-log` in `main.cjs`: `autoDownload=false`, checks on launch + every 6h (packaged only), emits status to the renderer over `frge:update`, IPC `frge:update-check/-download/-install/-state` + `frge:version`. The renderer shows an **Update pill top-right** (available → downloading% → Restart & update) and an **Updates row in Settings → General**. `quitAndInstall` sets `isQuiting` so the tray app exits to install. Config: `build.publish` = github Smiggul/pantheon-guide (the repo is now **PUBLIC**, so no token needed), which bakes `app-update.yml` into the package. electron-builder auto-bundles the prod deps into `resources/app/node_modules/` even though `files` is custom (verified). **Release process:** bump `package.json` version + roll CHANGELOG, `npm run dist:win`, then `gh release create vX.Y.Z --repo Smiggul/pantheon-guide --target frge-gg --notes-file <notes> "release/FRGE.GG Setup X.Y.Z.exe" "release/FRGE.GG X.Y.Z.exe" "release/latest.yml"`. **`latest.yml` MUST be attached** — it's what installed apps read to detect the new version. Auto-update only works from v0.7.0 forward (older installs need one manual update). Portable exe can't self-update; the NSIS installer is the updatable one. Still unsigned → SmartScreen warns until a cert is added.
- **Desktop auto-update** (original research 2026-08-06, now implemented above): use `electron-updater` (+ `electron-log`) — ~10 lines in `main.cjs` calling `autoUpdater.checkForUpdatesAndNotify()`; it fetches `latest.yml` (emitted by electron-builder next to the installer), compares to `package.json` version, downloads + installs on quit. **Windows auto-update needs the NSIS target (already built); the PORTABLE exe can't self-update** — so the installed/Setup build is the one to distribute. Blocker: the repo is **private**, so the plain GitHub-Releases provider would require shipping a GH token in every install (insecure). Hosting options, decision still OPEN: (a) a separate **public releases repo** holding only installers + `latest.yml`, source stays private (recommended — free, works with the `github` provider / `update.electronjs.org`); (b) **generic provider** → Cloudflare R2 / S3 bucket (free tier, full control, more setup); (c) embed a read-only token (NOT recommended). Signing: builds are unsigned, so SmartScreen warns on each update until a code-signing cert (or Azure Trusted Signing) is added — fine to defer for playtesting. Next step when resumed: pick a host, add `publish:` config + a release workflow (can extend the patch-update CI) that uploads the installer + `latest.yml` per version tag.
- **Ban ordering by real matchup win-rate** (data pass): the Recommended Ban tiles already render worst-first with a priority badge and the click-to-preselect plumbing, but the ordering currently follows each champ's authored ban list (worst-matchup-by-convention). A full 173-champ pass to verify/sort bans by actual matchup win-rate is still owed — a good candidate to source from a stats API (or the auto-patch workflow) rather than by hand. Pantheon Top is done as the worked example (Malphite › Gangplank › Jax).

### Monetisation / accounts (planned — not built)
- **Ad slots**: 4 non-intrusive zones are already reserved in the layout (top banner, left + right side rails, collapsible bottom bar). They render as placeholders today; wire a real ad network later. Ads must stay non-intrusive — never overlay the build/rune workspace.
- **User login + accounts**: track users, sync their saved builds/preferences. Auth belongs in the Electron **main** process; store tokens with Electron `safeStorage` (OS keychain) — never `localStorage`. The Settings popout already has the entry point (an "Account" section with a Sign in / Sign up button that currently only shows a "coming soon" note — no credential capture, on purpose). Real auth is **backend-gated** and needs, roughly:
  - A small backend service (auth server + DB) — the app is currently a static SPA + Electron shell with **no backend at all**. Nothing real can be built client-only.
  - **Riot account linking = Riot Sign-On (RSO)**, which is an OAuth2 flow — but RSO access is **granted by Riot per-product and is heavily gated** (not open to hobby apps by default; requires an application/approval). Do NOT assume it's available; confirm approval before building the flow. RSO can return the user's **email** (with the right scope), which is the intended source for the mailing-list opt-in.
  - **Mailing list**: needs the backend to store the email + an email provider (e.g. a transactional/marketing service). Opt-in must be explicit (a checkbox), never auto-subscribe.
  - The user plans to prototype accounts/payments in a **separate project first**; treat this as scaffold-only here until that lands.
- **VIP subscription**: a paid tier that hides all ads. The UI already gates every ad zone behind an `isVip` flag, so switching it off is a one-line change once auth exists.
- **Payment gateways to evaluate**: Yoco and Ozow (both South-Africa-first, good local card/EFT support), PayPal (international). Decision deferred — the user plans to prototype this in a separate project first.
- **Security prerequisite**: before shipping login/ads, run CodeRabbit over the PR and add a Content-Security-Policy + render ad content in a sandboxed iframe (external ad scripts must never run in the main renderer).