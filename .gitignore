# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
release
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright MCP browser artifacts
.playwright-mcp/

# local build-export test artifacts
exports/

# Data Dragon image assets — bundled at build time (populated by
# helpers/setup_ddragon.py), not meant to live in the repo. Prevents new
# spell/passive/champion/item icons from being accidentally staged.
# (Some were tracked before this rule; run `git rm -r --cached public/ddragon/img`
#  to untrack the rest when desired.)
public/ddragon/img/
